import React from "react";
import { Alert } from "react-bootstrap";

const PaymentCancel = () => {
  return <Alert variant="danger">Thanh toán xảy ra lỗi</Alert>;
};

export default PaymentCancel;
